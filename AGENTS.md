# Wind3DGS 필수 작업 지침

Root·`code/`·`ideas/`·`experiments/`는 독립 Git 저장소이며 submodule이 아니다. 정책·환경·manuscript는 root, 구현은 code, 연구 계약은 ideas, 실험·산출물은 experiments가 소유한다. 폴더·원격 경로는 [workspace 안내](README.md#project-repositories), 현행 방법은 [연구 진입점](ideas/README.md#현재-아이디어-스케치)을 따른다.

## 필수 구현·실행 원칙

- 최신 사용자 요청 범위를 지킨다. 확인·설명·진단은 읽기 전용이며 수정으로 확대하지 않는다. 승인된 구현·필요 검증은 진행하되 연구 방향·공용 계약·완료 기준·큰 계산 비용이나 범위 변경은 먼저 확인한다.
- 영향받는 저장소별 `git status --short --branch`와 변경할 파일의 실제 상태를 확인한다. 기존 dirty 변경, 원본 결과, ignored artifact와 실행 중인 작업은 사용자 소유다. 임의 삭제·덮어쓰기·중단·재시작·stash·reset하지 않는다.
- 구현·실행은 해당 계약과 독립 검산을 유지하고 위험도에 맞게 검증한다. 실패 분모를 보존하며 구현 완료·부분 검증·전체 완료를 구분한다. 기록의 다음 작업이나 이전 승인을 새 실행의 포괄적 허가로 간주하지 않는다.
- Stage는 검토한 정확한 경로만, commit은 저장소별 diff·검증 확인 후 수행한다. Push는 명시적 요청이 있을 때만 하며 force-push·tag·history rewrite는 별도 승인이 필요하다. 원격 최신성을 주장하려면 fetch 여부를 명시한다.
- Secret·credential·개인 절대 경로를 기록·commit하지 않는다. `.env`와 `.env.*`는 로컬 전용이며 공유용 `.env.example`에는 변수명·설명만 둔다. 프로젝트 밖 sibling 저장소를 만들지 않는다.

## 시작과 필요한 맥락 읽기

1. 공통 지침과 해당 폴더의 지침을 적용한다. 최신 요청과 확보한 맥락이 충분하면 새 채팅 여부와 관계없이 진행한다. 이미 제공되거나 확인한 내용을 재출력·재독하지 않는다.
2. 맥락이 부족하면 해당 폴더의 [작업 색인](README.md#작업-시작)에서 요청 주제의 최신 note를 찾아 `현재 상태`만 먼저 읽는다. 전체 기록의 최신 날짜로 작업을 고르지 않으며 모호한 후보가 여러 개면 확인한다.
3. 추가 정보는 note의 설명이 붙은 파일·절 링크를 따라 필요한 부분만 읽는다. 요청 범위·적용 규칙·판단 근거·검증 방법이 확보되면 멈춘다. 연결된 문서를 전부 재귀적으로 읽지 않는다.
4. `rg`는 알려진 문서 안에서 절·식별자를 찾거나 연결이 없거나 불충분할 때 쓰는 보조 수단이다. 날짜별 전량 읽기·네 저장소 이력 일괄 조회는 하지 않는다. 다른 저장소는 의존성이 있을 때만 참조한다.
5. 긴 로그는 실패·요약 구간, JSON은 필요한 필드, 코드는 관련 함수부터 읽는다. 도구 출력은 필요한 범위로 제한하고, 잘린 출력을 다시 전량 요청하지 않는다. 전체 감사는 요청된 범위에 한해 예외다.
6. 현재 프로세스·파일·버전 등 실행 전 확인과 아래 조건부 필수 절차는 생략하지 않는다. 기록이 오래됐거나 실제 상태와 다르면 차이를 확인하고, 과거 검증을 현재 버전의 결과로 승계하지 않는다.

## 간결한 작업 기록

- 구현 개선·방향 결정·검증 결과 확정·중단 등 의미 있는 상태 변화가 생기면 작업 구간 종료 시 기록한다. 긴 작업은 복구 가능한 중간 지점에서도 갱신하며 새 채팅·별도 정리 요청을 기다리지 않는다. 단순 읽기 전용 설명은 기록하지 않는다.
- 같은 작업은 기존 note를 갱신하고 독립 작업만 `YYYY-MM-DD_NN_short_topic.md`로 만든다. 해당 sessions 폴더·날짜의 두 자리 순번을 사용하고 기존 파일명은 유지한다. 변경된 소유 폴더만 기록한다.
- 앞부분 `현재 상태`에는 확인일·대상 식별 근거, 현재 단계, 채택 결정·이유·제약, 검증 범위, 미완료·불확실성, 다음 작업·사용자 선택과 근거 링크를 둔다. 10–20줄을 목표로 하되 중요한 한계를 생략하지 않는다.
- `작업 색인 → note의 현재 상태 → 상세 소유 문서·절 → 원본 설정·결과`로 연결한다. 각 링크는 **어떤 정보가 있는지 + 정확한 위치**를 명시한다. Markdown은 상대 경로와 절 anchor, 코드·TeX·JSON·로그는 파일 링크와 함수·label·key·run/frame 식별자를 쓴다. 키워드·파일명·줄 번호만 남기지 않는다.
- 색인은 주제별 최신 note·상태·한계만 짧게 유지한다. 상세 수치와 진행 이력을 복제하지 않고, 대체된 항목은 당시 상태로 보존하여 직접 연결한다. 기존 소유 문서가 있으면 계층을 채우기 위한 중복 문서를 만들지 않는다.
- 성공과 채택 결정을 먼저 쓰되 실패는 `조건 → 원인 → 해결 또는 재검토 조건`으로 보존한다. 새 결정이 이전 결정을 대체하는지 명시한다. 검증을 재실행하지 않았으면 이전 기록의 판정임을 밝힌다. 대화 원문·프레임 진행·시간순 시행착오는 남기지 않는다.
- 기록·경로·절을 변경하면 새 링크의 파일·절·식별자와 관련 진입 링크를 검증한다. 오래된 링크를 제거할 때는 참조를 갱신하거나 호환 anchor를 유지한다. 연결 누락은 해당 기록을 갱신하는 작업에서 보완한다.
- 최종 보고는 결과·검증·한계·기록 링크를 짧게 제시하고, 영향받은 저장소별 worktree·commit·push 상태와 외부 fetch/다운로드 여부를 구분한다.

## 기록의 소유권과 갱신

- 수식·방법·claim은 ideas sketch, 단계/Gate는 master, 파트 계약·완료 판정은 해당 R 문서가 소유한다. 실행 명령·config·revision/hash·전체 수치·실패 분모는 experiments report/manifest, 코드 API는 code 상세 문서가 소유한다.
- README는 진입점, session은 상태·결정·한계와 직접 링크만 관리한다. 확정 결과·계약이 바뀐 관련 문서만 갱신하며 과거 근거를 일괄 재작성하지 않는다. 연구·구현·실험 결과 정리에는 아래 R 동기화 절차를 적용한다.

## 조건부 필수 절차

해당 작업에 착수하기 전에 연결된 절을 확인한다. 관계없는 절차는 읽지 않는다. 이미 확인한 절은 변경·불확실성이 있을 때만 다시 읽는다.

| 작업 조건 | 반드시 확인할 상세 위치 |
| --- | --- |
| GPU teacher 신규 구현·정밀도·적분기·재시도·성능 | [GPU 작업별 계약 경로](code/docs/task_routes.md#gpu-작업)에서 해당 계약·검산·측정 절 |
| 복수 실행·자원 배분·속도 비교 | <a id="독립-작업의-병렬-처리"></a>[병렬 처리·자원 경쟁·측정 조건](docs/workflows/parallel_execution.md#독립-작업의-병렬-처리) |
| Stage·commit·push·원격 비교 | <a id="split-repository-rules"></a>[독립 저장소 Git 절차](docs/workflows/repository_operations.md#split-repository-rules) |
| `external/` 작업 | <a id="external-dependency-및-patch-규칙"></a>[manifest·clean 검증·patch 경계](docs/workflows/external_dependencies.md#external-dependency-및-patch-규칙) |
| 연구·구현·실험 결과 정리·인계 | <a id="정리-요청과-r-문서-동기화"></a>[관련 R 확인·완료 판정·보고](docs/workflows/research_and_latex.md#정리-요청과-r-문서-동기화) |
| 이번 작업의 `.tex` 수정 | <a id="latex-pdf-빌드-규칙"></a>[canonical PDF 빌드·bundle·실패 처리](docs/workflows/research_and_latex.md#latex-pdf-빌드-규칙) |
| 연구 방향 전환·canonical 경로 변경 | <a id="연구-방향-전환-절차"></a>[기존 근거 보존·전환·재검증](docs/workflows/research_and_latex.md#연구-방향-전환-절차) |

## 기록 언어 규칙

새로 쓰거나 갱신하는 README·session·report·설명 로그는 한국어를 기본으로 한다. 명령·식별자·공식 명칭·외부 도구 원문은 유지하며 기존 영문 기록은 소급 번역하지 않는다. 사용자가 명시한 영문 작성 요청은 따른다.

## 사용자 설명 방식: 개념 우선

효과와 개념을 먼저 설명하고 필요한 근거·수치·한계를 덧붙인다. 전문 용어는 처음에 쉽게 풀며, 상세 수식은 요청 시 제공한다. 쉽게 설명하더라도 구현·부분 검증·최종 채택을 구분하고 canonical 수식·계약은 유지한다.

<a id="작업-유형과-기록-기준"></a>
<a id="보안-및-로컬-경로"></a>
<a id="folder-roles"></a>
작업 유형·보안은 위 필수 원칙, 폴더와 경로는 [workspace 안내](README.md#project-repositories)를 따른다.
